{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust",
            "type": "gdb",
            "request": "launch",
            "gdbpath": "rust-gdb",
            "target": "${workspaceFolder}/target/debug/<program>",
            "arguments": [],
            "cwd": "${workspaceFolder}",
        }
    ]
}
