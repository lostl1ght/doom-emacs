{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug executable",
            "type": "lldb",
            "request": "launch",
            "target": "${workspaceFolder}/<program>",
            "args": [],
            "cwd": "${workspaceFolder}",
        }
    ]
}
